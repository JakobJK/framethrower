mutation instructorUpdateAvailability($availability: InstructorAvailability!) {
  instructorUpdateAvailability(input: { availability_: $availability }) {
    instructor {
      availability
      id
    }
  }
}
