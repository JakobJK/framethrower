query allCompanyAdminMonths($groupId: UUID!) {
  allVCompanyAdminMonths(filter: { groupId: { equalTo: $groupId } }) {
    nodes {
      year
      id
      month
      groupId
      amountFeedbacks
    }
  }
}
